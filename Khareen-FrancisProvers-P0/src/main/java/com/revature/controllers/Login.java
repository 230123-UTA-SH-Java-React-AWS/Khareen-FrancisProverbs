package com.revature.controllers;

public class Login {

    /*
    1. Create a login endpoint
    2. Call login endpoint with email and password in request body(plain text for now, but this should be encrypted someway)
    3. Validate credentials and return JSON web token
    4. Save web token in app and use it in request header for subsequent API calls
    */
}
